# Windows 部署脚本必须以 CRLF 换行签出。
# 这些文件同时带 UTF-8 BOM：Windows PowerShell 5.1 对无 BOM 的 UTF-8 会按系统 ANSI
# 代码页（中文系统为 GBK）解码，脚本里的中文会导致解析失败。BOM 属于文件内容，
# git 不会改动；换行符由下面的规则固定。
*.ps1 text eol=crlf
*.bat text eol=crlf

# 面向 macOS / Linux 构建机的脚本保持 LF。
*.sh text eol=lf
